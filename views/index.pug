extends layout

block content
  h1= title
  p Welcome to pug assignment  #{title}
  button(type="button",onclick="update()",class="btn btn-secondary") PressMe
  <br>

  div(id="target") Display
  script.
        let a=0;
        function update()
         {
            a=a+1;
            document.getElementById("target").innerHTML =a;

         };
  a(href="https://seshasaipug1.herokuapp.com/volkswagen") link to array        